#!/bin/bash

echo "Deprecated, make this work with `ykman`"

# if [ -z "$1" ]
#   then
#     echo "No argument supplied"
#     exit
# fi
# 
# otp=$(/usr/bin/yubioath show -s1 6 $1 | sed -r 's!.*\s!!')
# echo $otp | wl-copy --trim-newline
